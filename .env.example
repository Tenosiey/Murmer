# Murmer configuration template
# Copy to .env and adjust values for local development.

# === Server settings ===
DATABASE_URL=postgres://murmer:murmer@localhost:5432/murmer
UPLOAD_DIR=./uploads
# Optional shared secret that clients must present during authentication
#SERVER_PASSWORD=
# Optional bearer token that unlocks the /role management endpoint
#ADMIN_TOKEN=
# Bind address for the server (defaults to 0.0.0.0:3001 if unset)
#BIND_ADDRESS=0.0.0.0:3001
# Comma-separated origins allowed to issue cross-origin requests (omit in production)
#CORS_ALLOW_ORIGINS=http://localhost:1420
# Rate limiting configuration
MAX_MESSAGES_PER_MINUTE=120
MAX_AUTH_ATTEMPTS_PER_MINUTE=5
NONCE_EXPIRY_SECONDS=300

# === Client settings ===
# Location of the Murmer server the desktop app should talk to in development.
#MURMER_SERVER_URL=ws://localhost:3001/ws

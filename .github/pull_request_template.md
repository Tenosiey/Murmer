<!--
Keep it to one coherent change. See CONTRIBUTING.md.
-->

## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How it was verified

<!--
Name what you actually ran and looked at, not just "tested".
- [ ] cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test
- [ ] bun run check (0 errors, 0 warnings) && bun run test
- [ ] Looked at it in the running app (say which surface, and both themes if visual)
- [ ] Smoke-tested by hand (networking / auth / uploads changes only)
-->

## Anything worth flagging

<!--
Delete what does not apply:
- Documentation updated (README / AGENTS.md / docs/)
- Touches permissions — the server-side check is where?
- Touches encryption or key handling
- Adds a constant the other half also defines (see agents/skills/mirrored-constants.md)
- Changes the database schema
-->
